#include<stdio.h>
int main()
{
        int x, y, mul, div;

    scanf("%d %d", &x, &y);

    mul = x*y;

    printf("Multiplication: %d \n", mul);

    div = x/y;

    printf("Division: %d", div);
}
